
When(/^the user adds the lock entry \"(.*)\"$/, () => { });
When(/^the user removes the lock entry \"(.*)\"$/, () => { });
Then('an alert box appears', () => { });
When(/^an alert box with the text \"(.*)\" appears$/, () => { });
When(/^an alert box with the text containing \"(.*)\" appears$/, () => { });
When(/^an alert box appears with the title \"(.*)\"$/, () => { });
When(/^the user clicks the \"(.*)\" opinion in the alert box$/, () => { });
When(/^an alert box with the text containing \"(.*)\" and the stored text value appears$/, () => { });
When(/^the user clicks the \"(.*)\" option in the alert box$/, () => { });
When(/^the user dismisses the alert box$/, () => { });
When(/^the user selects the main data table of the page$/, () => { });
When(/^the user selects the main fixed data table of the page$/, () => { });
When(/^the user selects the data table with selector: \"(.*)\"$/, () => { });
When(/^the user selects header data table with selector: \"(.*)\"$/, () => { });
When(/^the user selects body data table with selector: \"(.*)\"$/, () => { });
When(/^the user selects the data table with x3 field name: \"(.*)\"$/, () => { });
When(/^the user selects the data table in the popup$/, () => { });
When(/^the user selects the fixed data table in the popup$/, () => { });
When(/^the user selects the data table of section: \"(.*)\"$/, () => { });
When(/^the user selects the fixed data table of section: \"(.*)\"$/, () => { });
When(/^the user selects the data table of field: \"(.*)\"$/, () => { });
When(/^the user selects the fixed data table for x3 field name: \"(.*)\"$/, () => { });
When(/^the user selects the scrolling data table for x3 field name: \"(.*)\"$/, () => { });
When(/^the user selects the data table of left panel$/, () => { });
When(/^the user selects row that has the text \"(.*)\" in column with header: \"(.*)\"$/, () => { });
When(/^the user selects row by multiple criteria that has the text \"(.*)\" in column with header: \"(.*)\" and the text \"(.*)\" in column with header: \"(.*)\"$/, () => { });
When(/^the user selects row that has stored text with the key: \"(.*)\" in column with header: \"(.*)\"$/, () => { });
When(/^the user selects row that has the text \"(.*)\" in column with index: \"(.*)\"$/, () => { });
When(/^the user selects last row of the selected data table$/, () => { });
When(/^the user selects first row of the selected data table$/, () => { });
When(/^the user selects row that has the text \"(.*)\" in column with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects row by multiple criteria that has the text \"(.*)\" in column with X3 field name: \"(.*)\" and the text \"(.*)\" in column with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects editable row that has the text \"(.*)\" in column with header: \"(.*)\"$/, () => { });
When(/^the user selects editable row that has the text \"(.*)\" in column with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects editable table row number: (.*)$/, () => { });
When(/^the user selects editable row that has the stored text with the key: \"(.*)\" in column with header: \"(.*)\"$/, () => { });
When(/^the user selects editable row that has the stored text with the key: \"(.*)\" in column with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects fixed table row number: (.*) in column with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects fixed table row number: (.*)$/, () => { });
When(/^the user selects fixed table row that has the stored value in column with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects table row that is above the currently selected row$/, () => { });
When(/^the user selects table row that is below the currently selected row$/, () => { });
When(/^the user selects cell with column header: \"(.*)\" and row number: (.*)$/, () => { });
When(/^the user selects cell that may not be visible with column header: \"(.*)\" and row number: (.*)$/, () => { });
When(/^the user selects the disabled cell with column header: \"(.*)\" and row number: (.*)$/, () => { });
When(/^the user selects last cell with column header: \"(.*)\"$/, () => { });
When(/^the user selects cell with text: \"(.*)\" and column header: \"(.*)\"$/, () => { });
When(/^the user selects cell that matches exact with the text: \"(.*)\" and column header: \"(.*)\"$/, () => { });
When(/^the user selects cell that matches exact with the text: \"(.*)\" and X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects cell that matches exact with the stored text with the key: \"(.*)\" and column header: \"(.*)\"$/, () => { });
When(/^the user clicks on the cell that has the text \"(.*)\" in cell with header: \"(.*)\"$/, () => { });
When(/^the user selects last editable cell with column header: \"(.*)\"$/, () => { });
When(/^the user selects last editable cell with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects the fixed cell with X3 field name: \"(.*)\" and row number: (.*)$/, () => { });
When(/^the user selects last fixed cell with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects last fixed cell with header: \"(.*)\"$/, () => { });
When(/^the user selects search cell with header: \"(.*)\"$/, () => { });
When(/^the user selects cell with header: \"(.*)\" of selected row$/, () => { });
When(/^the user selects cell with X3 field name: \"(.*)\" of selected row$/, () => { });
When(/^the user selects fixed cell with X3 field name: \"(.*)\" of selected row$/, () => { });
When(/^the user selects hierarchical cell of selected row$/, () => { });
When(/^the user selects hierarchical cell with text: \"(.*)\"$/, () => { });
When(/^the user selects hierarchical cell with the stored text with key: \"(.*)\"$/, () => { });
When(/^the user selects row that has the hierarchical cell with text: \"(.*)\"$/, () => { });
When(/^the user selects row that has the hierarchical cell with the stored text with key: \"(.*)\"$/, () => { });
When(/^the user adds the text \"(.*)\" in selected cell$/, () => { });
When(/^the user adds the text \"(.*)\" in selected cell and hits enter key$/, () => { });
When(/^the selected data table of the left panel is empty$/, () => { });
When(/^the selected picking list of the left panel is empty$/, () => { });
When(/^the user adds the text \"(.*)\" in selected cell and hits tab key$/, () => { });
When(/^the user adds the stored text with key \"(.*)\" in selected cell$/, () => { });
When(/^the user adds the stored text with key \"(.*)\" in selected cell and hits enter key$/, () => { });
When(/^the user adds the stored text with key \"(.*)\" in selected cell and hits tab key$/, () => { });
When(/^the user edits text to \"(.*)\" for cell with X3 field name: \"(.*)\" of selected row$/, () => { });
When(/^the user edits text to \"(.*)\" for cell with X3 field name: \"(.*)\" of selected row and hits enter$/, () => { });
When(/^the user edits text to \"(.*)\" for cell with header: \"(.*)\" of selected row$/, () => { });
When(/^the user enters todays date in the selected cell$/, () => { });
When(/^the user enters a generated date using the value \"(.*)\" in the selected cell$/, () => { });
When(/^the user enters the first day of the month in the selected cell$/, () => { });
When(/^the user enters the first day of the month in the selected cell and adjusts by \"(.*)\"$/, () => { });
When(/^the user enters the first day of the year in the selected cell$/, () => { });
When(/^the user enters the first day of the year in the selected cell and adjusts by \"(.*)\"$/, () => { });
When(/^the user enters the last day of the month in the selected cell$/, () => { });
When(/^the user enters the last day of the month in the selected cell and adjusts by \"(.*)\"$/, () => { });
When(/^the user enters the last day of the year in the selected cell$/, () => { });
When(/^the user clicks the \"(.*)\" button in the left top bar of the selected data table$/, () => { });
When(/^the user enters the last day of the year in the selected cell and adjusts by \"(.*)\"$/, () => { });
When(/^the user enters the day in the selected cell$/, () => { });
When(/^the user enters the month in the selected cell$/, () => { });
When(/^the user enters the year in the selected cell$/, () => { });
When(/^the user enters the week number in the selected cell$/, () => { });
When(/^the user enters the current tax year starting \"(.*)\" in the selected cell$/, () => { });
When(/^the user enters the \"(.*)\" from today in the selected cell$/, () => { });
When(/^the user clicks on the selected cell$/, () => { });
When(/^the user clicks on the selected row$/, () => { });
When(/^the user hits enter key in the selected cell$/, () => { });
When(/^the user hits tab key in the selected cell$/, () => { });
When(/^the user hits escape key in the selected cell$/, () => { });
When(/^the user ticks the select all checkbox contained in the column header$/, () => { });
When(/^the user unticks the select all checkbox contained in the column header$/, () => { });
When(/^the user ticks the checkbox contained in the selected cell$/, () => { });
When(/^the user unticks the checkbox contained in the selected cell$/, () => { });
When(/^the user clicks on the icon contained in the selected cell$/, () => { });
When(/^the user opens \"(.*)\" main function of the selected row$/, () => { });
When(/^the user opens \"(.*)\" function on toolbox of the selected row$/, () => { });
When(/^the user opens \"(.*)\" with stored value function on toolbox of the selected row$/, () => { });
When(/^the user clicks on the link in the cell that has the text \"(.*)\" in cell with header: \"(.*)\"$/, () => { });
When(/^the user sets rows to display to (.*)$/, () => { });
When(/^the user clicks the \"(.*)\" button at the top of the table$/, () => { });
When(/^the user selects the choice \"(.*)\" of the selected cell$/, () => { });
When(/^the user clicks the \"(.*)\" action button of the selected cell$/, () => { });
When(/^the user clicks the \"(.*)\" action in the left top bar of the selected data table$/, () => { });
When(/^the user clicks on the cell that matches exact with the text \"(.*)\" in cell with header: \"(.*)\"$/, () => { });
When(/^the user clicks the \"(.*)\" option of the actions panel for the selected cell$/, () => { });
When(/^the user highlights the selected row$/, () => { });
When(/^the value of the selected cell is stored$/, () => { });
When(/^the user stores the value of the selected cell with the key: \"(.*)\"$/, () => { });
When(/^the user checks the selected row$/, () => { });
When(/^the user clicks the \"(.*)\" button in the right top bar of the selected data table$/, () => { });
When(/^the number of rows returned in the selected data table is \"(.*)\"$/, () => { });
When(/^the user clicks the \"(.*)\" button in the right top bar of the selected picking list$/, () => { });
When(/^the user expands the hierarchical list of the selected cell$/, () => { });
When(/^the user collapses the hierarchical list of the selected cell$/, () => { });
When(/^the value of the selected cell is \"(.*)\"$/, () => { });
When(/^the value of the selected cell not is \"(.*)\"$/, () => { });
When(/^the value of the selected cell matches the stored text with key \"(.*)\"$/, () => { });
When(/^the user checks if there is value: \"(.*)\" in the selected row$/, () => { });
When(/^the value of the selected cell contains \"(.*)\"$/, () => { });
When(/^the value of the selected cell has string pattern \"(.*)\"$/, () => { });
When(/^the selected cell is empty$/, () => { });
When(/^the selected cell is not empty$/, () => { });
When(/^the selected cell is enabled$/, () => { });
When(/^the selected cell is disabled$/, () => { });
When(/^the choice selected of the selected cell is \"(.*)\"$/, () => { });
When(/^the \"(.*)\" choice of the selected cell is displayed$/, () => { });
When(/^the \"(.*)\" choice of the selected cell is hidden$/, () => { });
When(/^the value of the selected cell is todays date$/, () => { });
When(/^the value of the selected cell is a generated date using value \"(.*)\"$/, () => { });
When(/^the value of the selected cell is the first day of the month$/, () => { });
When(/^the value of the selected cell is the first day of the month adjusted by \"(.*)\"$/, () => { });
When(/^the value of the selected cell is the first day of the year$/, () => { });
When(/^the value of the selected cell is the first day of the year adjusted by \"(.*)\"$/, () => { });
When(/^the value of the selected cell is the last day of the month$/, () => { });
When(/^the value of the selected cell is the last day of the month adjusted by \"(.*)\"$/, () => { });
When(/^the value of the selected cell is the last day of the year$/, () => { });
When(/^the value of the selected cell is the last day of the year adjusted by \"(.*)\"$/, () => { });
When(/^the value of the selected cell is the current day$/, () => { });
When(/^the value of the selected cell is the current month$/, () => { });
When(/^the value of the selected cell is the current year$/, () => { });
When(/^the value of the selected cell is the current week number$/, () => { });
When(/^the value of the selected cell is the current tax year starting \"(.*)\"$/, () => { });
When(/^the value of the selected cell is the \"(.*)\" from today$/, () => { });
When(/^the value of the selected cell matches \"(.*)\" where ~date~ is a generated date using value \"(.*)\"$/, () => { });
When(/^the value of the checkbox contained in the selected cell is ticked$/, () => { });
When(/^the value of the checkbox contained in the selected cell is unticked$/, () => { });
When(/^the color of the text in the selected cell is \"(.*)\"$/, () => { });
When(/^the color of the background in the selected cell is \"(.*)\"$/, () => { });
When(/^the column header with name \"(.*)\" of the selected data table is displayed$/, () => { });
When(/^the column header with name \"(.*)\" of the selected data table is hidden$/, () => { });
When(/^the column header with X3 field name \"(.*)\" of the selected data table is displayed$/, () => { });
When(/^the column header with X3 field name \"(.*)\" of the selected data table is hidden$/, () => { });
Then('a dialog box appears', () => { });
When(/^a confirmation dialog appears with the message \"(.*)\"$/, () => { });
When(/^a confirmation dialog appears containing the message \"(.*)\"$/, () => { });
When(/^the user clicks \"(.*)\" button on the confirmation dialog$/, () => { });
Then('an alert box does not appear', () => { });
When(/^the user selects the text field with name: \"(.*)\"$/, () => { });
When(/^the user selects the text field with X3 field name: \"(.*)\"$/, () => { });
When(/^the text field with name: \"(.*)\" is displayed$/, () => { });
When(/^the text field with name: \"(.*)\" is hidden$/, () => { });
When(/^the text field with X3 field name: \"(.*)\" is displayed$/, () => { });
When(/^the text field with X3 field name: \"(.*)\" is hidden$/, () => { });
When(/^the icon field with name: \"(.*)\" is displayed$/, () => { });
When(/^the icon field with name: \"(.*)\" is hidden$/, () => { });
When(/^the icon field with X3 field name: \"(.*)\" is displayed$/, () => { });
When(/^the icon field with X3 field name: \"(.*)\" is hidden$/, () => { });
When(/^the tab with title \"(.*)\" is displayed$/, () => { });
When(/^the tab with title \"(.*)\" is hidden$/, () => { });
When(/^the function \"(.*)\" in the site map is displayed$/, () => { });
When(/^the function \"(.*)\" in the site map is hidden$/, () => { });
When(/^the entity \"(.*)\" in the site map is displayed$/, () => { });
When(/^the entity \"(.*)\" in the site map is hidden$/, () => { });
When(/^the link field with name: \"(.*)\" is displayed$/, () => { });
When(/^the link field with name: \"(.*)\" is hidden$/, () => { });
When(/^the link field with X3 field name: \"(.*)\" is displayed$/, () => { });
When(/^the link field with X3 field name: \"(.*)\" is hidden$/, () => { });
When(/^the user selects the text editor of section: \"(.*)\"$/, () => { });
When(/^the user selects the text editor with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects the date field with name: \"(.*)\"$/, () => { });
When(/^the user selects the date field with X3 field name: \"(.*)\"$/, () => { });
When(/^the date field with name: \"(.*)\" is displayed$/, () => { });
When(/^the date field with name: \"(.*)\" is hidden$/, () => { });
When(/^the date field with X3 field name: \"(.*)\" is displayed$/, () => { });
When(/^the date field with X3 field name: \"(.*)\" is hidden$/, () => { });
When(/^the text editor field of section: \"(.*)\" is displayed$/, () => { });
When(/^the text editor field of section: \"(.*)\" is hidden$/, () => { });
When(/^the text editor field with X3 field name: \"(.*)\" is displayed$/, () => { });
When(/^the text editor field with X3 field name: \"(.*)\" is hidden$/, () => { });
When(/^the password field with name: \"(.*)\" is displayed$/, () => { });
When(/^the password field with name: \"(.*)\" is hidden$/, () => { });
When(/^the check box field with name: \"(.*)\" is displayed$/, () => { });
When(/^the check box field with name: \"(.*)\" is hidden$/, () => { });
When(/^the check box field with X3 field name: \"(.*)\" is displayed$/, () => { });
When(/^the check box field with X3 field name: \"(.*)\" is hidden$/, () => { });
When(/^the user selects the check box with name: \"(.*)\"$/, () => { });
When(/^the user selects the check box with X3 field name: \"(.*)\"$/, () => { });
When(/^the drop down list with name: \"(.*)\" is displayed$/, () => { });
When(/^the drop down list with name: \"(.*)\" is hidden$/, () => { });
When(/^the drop down list with X3 field name: \"(.*)\" is displayed$/, () => { });
When(/^the drop down list with X3 field name: \"(.*)\" is hidden$/, () => { });
When(/^the user selects the drop down list with name: \"(.*)\"$/, () => { });
When(/^the user selects the drop down list with X3 field name: \"(.*)\"$/, () => { });
When(/^the radio buttons group with name: \"(.*)\" is displayed$/, () => { });
When(/^the radio buttons group with name: \"(.*)\" is hidden$/, () => { });
When(/^the radio buttons group with X3 field name: \"(.*)\" is displayed$/, () => { });
When(/^the radio buttons group with X3 field name: \"(.*)\" is hidden$/, () => { });
When(/^the user selects the radio buttons group with name: \"(.*)\"$/, () => { });
When(/^the user selects the radio buttons group with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects the link field with name: \"(.*)\"$/, () => { });
When(/^the user selects the link field with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects the icon field with name: \"(.*)\"$/, () => { });
When(/^the user selects the icon field with X3 field name: \"(.*)\"$/, () => { });
When(/^the user selects the password fields with name: \"(.*)\"$/, () => { });
When(/^the user writes \"(.*)\" to the selected field$/, () => { });
When(/^the user writes the day to the selected text field$/, () => { });
When(/^the user writes the month to the selected text field$/, () => { });
When(/^the user writes the year to the selected text field$/, () => { });
When(/^the user writes the week number to the selected text field$/, () => { });
When(/^the user writes the current Tax year starting on \"(.*)\" to the selected text field$/, () => { });
When(/^the user writes \"(.*)\" to the selected text area$/, () => { });
When(/^the user writes block of text to the selected text area$/, () => { });
When(/^the user writes \"(.*)\" to the selected text field$/, () => { });
When(/^the user writes \"(.*)\" to the selected text field with (.*) milliseconds writing delay$/, () => { });
When(/^the user writes \"(.*)\" suffixed with the build number to the selected text field and hits tab key$/, () => { });
When(/^the user writes \"(.*)\" to the selected text field and hits tab key$/, () => { });
When(/^the user writes \"(.*)\" to the selected text field and hits enter key$/, () => { });
When(/^the value of the selected text field is stored$/, () => { });
When(/^the user writes the stored text value to the selected text field and hits tab key$/, () => { });
When(/^the user stores the value of the selected text field with the key: \"(.*)\"$/, () => { });
When(/^the user stores the value of the selected date field with the key: \"(.*)\"$/, () => { });
When(/^the user writes the stored text with key \"(.*)\" in the selected text field$/, () => { });
When(/^the user writes the stored text with key \"(.*)\" in the selected text field and hits tab key$/, () => { });
When(/^the user writes the stored text with key \"(.*)\" in the selected text field and hits enter key$/, () => { });
When(/^the user writes block of text to the selected text editor$/, () => { });
When(/^the value of the selected text editor is$/, () => { });
When(/^the value of the selected text editor contains$/, () => { });
When(/^the value of the selected text field is \"(.*)\"$/, () => { });
When(/^the value of the selected text field contains \"(.*)\"$/, () => { });
When(/^the value of the selected text field has string pattern \"(.*)\"$/, () => { });
When(/^the value of the selected text area contains$/, () => { });
When(/^the value of the selected text field matches the stored cell value$/, () => { });
When(/^the value of the selected text field matches the stored text value$/, () => { });
When(/^the value of the selected text field matches the stored text with key \"(.*)\"$/, () => { });
When(/^the value of the selected text field is empty$/, () => { });
When(/^the value of the selected text field is the current day$/, () => { });
When(/^the value of the selected text field is the current month$/, () => { });
When(/^the value of the selected text field is the current year$/, () => { });
When(/^the value of the selected text field is the current week number$/, () => { });
When(/^the value of the selected text field is the current Tax year that starts on \"(.*)\"$/, () => { });
When(/^the value of the selected text field matches \"(.*)\" where ~date~ is a generated date using value \"(.*)\"$/, () => { });
When(/^the user clicks the \"(.*)\" button of the selected text field$/, () => { });
When(/^the user clicks the \"(.*)\" option of the actions panel$/, () => { });
When(/^the user clicks the \"(.*)\" button on the \"(.*)\" text field$/, () => { });
When(/^the selected text field should be enabled$/, () => { });
When(/^the selected text field should be disabled$/, () => { });
When(/^the user waits for a report printout with message \"(.*)\"$/, () => { });
When(/^the user selects the label field with name: \"(.*)\"$/, () => { });
When(/^the user asserts that the text field has desired value: \"(.*)\"$/, () => { });
When(/^the user writes \"(.*)\" to the selected date field$/, () => { });
When(/^the user writes today to the selected date field$/, () => { });
When(/^the user writes month: \"(.*)\" day: \"(.*)\" and year: \"(.*)\" to the selected date field$/, () => { });
When(/^the user writes default date to the selected date field$/, () => { });
When(/^the user writes a generated date in the selected date field using the value \"(.*)\"$/, () => { });
When(/^the user writes first day of month to the selected date field$/, () => { });
When(/^the user writes first day of month to the selected date field and adjusts it by \"(.*)\"$/, () => { });
When(/^the user writes last day of month to the selected date field$/, () => { });
When(/^the user writes last day of month to the selected date field and adjusts it by \"(.*)\"$/, () => { });
When(/^the user writes first day of the year to the selected date field$/, () => { });
When(/^the user writes first day of the year to the selected date field and adjusts it by \"(.*)\"$/, () => { });
When(/^the user writes last day of the year to the selected date field$/, () => { });
When(/^the user writes last day of the year to the selected date field and adjusts it by \"(.*)\"$/, () => { });
When(/^the user writes last day of \"(.*)\" to the selected date field$/, () => { });
When(/^the user writes the \"(.*)\" from today to the selected date field$/, () => { });
When(/^the value of the selected date field is month: \"(.*)\" day: \"(.*)\" and year: \"(.*)\"$/, () => { });
When(/^the value of the selected date field is \"(.*)\"$/, () => { });
When(/^the value of the selected date field is today$/, () => { });
When(/^the value of the selected date field is a generated date using value \"(.*)\"$/, () => { });
When(/^the value of the selected date field is the last day of the month$/, () => { });
When(/^the value of the selected date field is last day of the month adjusted by \"(.*)\"$/, () => { });
When(/^the value of the selected date field is the last day of the year$/, () => { });
When(/^the value of the selected date field is last day of the year adjusted by \"(.*)\"$/, () => { });
When(/^the value of the selected date field is the first day of the month$/, () => { });
When(/^the value of the selected date field is first day of the month adjusted by \"(.*)\"$/, () => { });
When(/^the value of the selected date field is the first day of the year$/, () => { });
When(/^the value of the selected date field is first day of the year adjusted by \"(.*)\"$/, () => { });
When(/^the value of the selected date field is the \"(.*)\" from today$/, () => { });
When(/^the user clicks on the selected check box$/, () => { });
When(/^the user sets the check box to ticked$/, () => { });
When(/^the user sets the check box to unticked$/, () => { });
When(/^the selected check box is checked$/, () => { });
When(/^the selected check box is not checked$/, () => { });
When(/^the selected check box is unchecked$/, () => { });
When(/^the selected check box is enabled$/, () => { });
When(/^the selected check box is disabled$/, () => { });
When(/^the user clicks on \"(.*)\" option of the selected drop down list$/, () => { });
When(/^the user clicks on option that matches exact with the text "(.*)" of the selected drop down list$/, () => { });
When(/^the user selects the text field of the selected drop down list$/, () => { });
When(/^the value of the selected drop down list is \"(.*)\"$/, () => { });
When(/^the \"(.*)\" value of the selected drop down list is displayed$/, () => { });
When(/^the \"(.*)\" value of the selected drop down list is hidden$/, () => { });
When(/^the user clicks on \"(.*)\" radio button of the selected radio buttons group$/, () => { });
When(/^the user clicks on \"(.*)\" radio button of the selected radio buttons group and dismisses the alert box$/, () => { });
When(/^the radio button \"(.*)\" of the selected radio buttons group is selected$/, () => { });
When(/^the radio button \"(.*)\" of the selected radio buttons group is not selected$/, () => { });
When(/^the radio button \"(.*)\" of the selected radio buttons group is disabled$/, () => { });
When(/^the radio button \"(.*)\" of the selected radio buttons group is enabled$/, () => { });
When(/^the user clicks on the selected link field$/, () => { });
When(/^the user clicks on \"(.*)\" button of the selected link field$/, () => { });
When(/^the text of the selected link field is \"(.*)\"$/, () => { });
When(/^the description of the selected link field is \"(.*)\"$/, () => { });
When(/^the user clicks on the selected icon field$/, () => { });
When(/^the user clicks on the \"(.*)\" icon of the selected icon field$/, () => { });
When(/^the icon of the selected icon field is checked$/, () => { });
When(/^the icon of the selected icon field is a cross$/, () => { });
When(/^the selected icon field should be disabled$/, () => { });
When(/^the value of the \"(.*)\" text field is \"(.*)\"$/, () => { });
When(/^the value of the \"(.*)\" date field is month: \"(.*)\" date: \"(.*)\" and year: \"(.*)\"$/, () => { });
When(/^the user writes \"(.*)\" password to the selected password field$/, () => { });
When(/^the user writes \"(.*)\" password to the selected password confirmation field$/, () => { });
When(/^the user loads \"(.*)\" URL$/, () => { });
When(/^the user selects item \"(.*)\" by css selector$/, () => { });
When(/^the user selects item \"(.*)\" by xpath$/, () => { });
When(/^the user selects item \"(.*)\" by link text$/, () => { });
When(/^the user selects item \"(.*)\" by id$/, () => { });
When(/^the user switches to the first opened tab$/, () => { });
When(/^the user switches to the last opened tab$/, () => { });
When(/^the user selects item \"(.*)\" by name$/, () => { });
When(/^the user selects item \"(.*)\" by class$/, () => { });
When(/^the user selects item \"(.*)\" by label text$/, () => { });
When(/^the user selects item \"(.*)\" by X3 name$/, () => { });
When(/^the user selects item with mask \"(.*)\" and field name \"(.*)\" by X3 name$/, () => { });
When(/^the user writes text \"(.*)\" on selected item$/, () => { });
When(/^the user click on selected item$/, () => { });
When(/^the user hits enter$/, () => { });
When(/^the user hits tab$/, () => { });
When(/^the user hits escape$/, () => { });
When(/^the text \"(.*)\" is shown in selected item$/, () => { });
When(/^the user waits$/, () => { });
When(/^the user waits (.*) seconds$/, () => { });
When(/^the user stores the generated value with length (\b([5-9]|1[0-3])\b) with the key "(.*)"$/, () => { });
When(/^the user opens the application on a desktop using the following link: \"(.*)\"$/, () => { });
When(/^the user writes \"(.*)\" GraphQL request/, () => { });
When(/^the user clicks the \"(.*)\" button in the GraphQL page header$/, () => { });
When(/^the \"(.*)\" GraphQL response is valid/, () => { });
When(/^the user selects the \"(.*)\" GraphQL property$/, () => { });
When(/^the selected GraphQL property value (is|is not|contains) \'(.*)\'$/, () => { });
When(/^the selected GraphQL property value (is|is not|contains)$/, () => { });
When(/^the user attaches the actual GraphQL response to allure report$/, () => { });
When(/^the user selects the \"(.*)\" user landing page$/, () => { });
When(/^the user selects the \"(.*)\" standard landing page$/, () => { });
When(/^the page Landing Page title changes to \"(.*)\"$/, () => { });
When(/^the landing page is displayed$/, () => { });
Then('a log panel appears', () => { });
When(/^the user selects the main log panel of the page$/, () => { });
When(/^the user sets rows in log panel to display to (.*)$/, () => { });
When(/^the user selects the log panel line containing text \"(.*)\"$/, () => { });
When(/^the user selects the log panel line containing text \"(.*)\" occurrence ([1-9]|10)$/, () => { });
When(/^the user extracts the value from the selected log panel line starting at (.*) for (.*) characters and stores it in key \"(.*)\"$/, () => { });
When(/^the selected log panel includes the message \"(.*)\"$/, () => { });
When(/^the selected log panel includes the confirmation message \"(.*)\"$/, () => { });
When(/^the selected log panel includes the error message \"(.*)\"$/, () => { });
When(/^the selected log panel line has string pattern \"(.*)\"$/, () => { });
When(/^the selected log panel line does not have string pattern \"(.*)\"$/, () => { });
When(/^the number of rows returned in the log panel is \"(.*)\"$/, () => { });
When(/^the user is logged into Sage X3 with \"(.*)\" using user name \"(.*)\" and password \"(.*)\"$/, () => { });
When(/^the modal dialog \"(.*)\" is displayed$/, () => { });
When(/^the user opens the \"(.*)\" function$/, () => { });
When(/^the user opens the \"(.*)\" entity$/, () => { });
Then('the user opens the sitemap', () => { });
When(/^the user opens the profile panel$/, () => { });
When(/^the user clicks on the left function \"(.*)\" on profile panel$/, () => { });
When(/^the user clicks on the toggle button with the text \"(.*)\"$/, () => { });
When(/^the user enables responsive design$/, () => { });
When(/^the user changes the main language to \"(.*)\"$/, () => { });
When(/^the user changes the language to \"(.*)\" in the more languages panel$/, () => { });
When(/^the user changes the main language code to \"(.*)\"$/, () => { });
When(/^the user changes the language code to \"(.*)\" in the more languages panel$/, () => { });
When(/^the user clicks on the site depositor action in the profile bar$/, () => { });
When(/^the user opens the \"(.*)\" entry of Bookmark dropdown panel$/, () => { });
When(/^the user opens the \"(.*)\" entry of Bookmark main panel$/, () => { });
When(/^the user clicks on Bookmark icon to add the screen to favorites$/, () => { });
When(/^the user clicks on Bookmark icon to remove the screen of favorites$/, () => { });
When(/^the favorites toolbar is collapsed$/, () => { });
When(/^the favorites toolbar is not collapsed$/, () => { });
When(/^the favorites toolbar is present$/, () => { });
When(/^the favorites toolbar is not present$/, () => { });
When(/^the \"(.*)\" screen is displayed$/, () => { });
When(/^the user logs-out from the system$/, () => { });
When(/^the user goes to the main page$/, () => { });
When(/^the user open the main page clicking on the logo$/, () => { });
When(/^the user selects the \"(.*)\" entry on endpoint panel$/, () => { });
When(/^the user selects the \"(.*)\" entry on endpoint panel if it exists$/, () => { });
When(/^the user selects the \"(.*)\" entry on the more endpoints panel option$/, () => { });
When(/^the \"(.*)\" endpoint is selected$/, () => { });
When(/^the user adds the file \"(.*)\" to the file upload component$/, () => { });
When(/^the user adds the file \"(.*)\" to the \"(.*)\" file upload component$/, () => { });
When(/^the user selects the main hierarchical panel of the screen$/, () => { });
When(/^the user selects the table hierarchical panel of the screen$/, () => { });
When(/^the user selects the item \"(.*)\" of the hierarchical panel$/, () => { });
When(/^the user selects the item \"(.*)\" of the level (.*) on the hierarchical panel$/, () => { });
When(/^the user selects the item \"(.*)\" of the hierarchical panel next level$/, () => { });
When(/^the user clicks on the selected hierarchical panel item$/, () => { });
When(/^the user expands the selected hierarchical panel item$/, () => { });
When(/^the user selects the main picking list panel of the screen$/, () => { });
When(/^the user selects the item \"(.*)\" of the picking list panel$/, () => { });
When(/^the user selects the item \"(.*)\" of the picking list panel next level$/, () => { });
When(/^the user selects the item containing string pattern \"(.*)\" of the picking list panel$/, () => { });
When(/^the user selects the item with the stored text with key \"(.*)\" and with the text containing \"(.*)\" of the picking list panel$/, () => { });
When(/^the user selects the item \"(.*)\" of the level (.*) on the picking list panel$/, () => { });
When(/^the user selects the item containing string pattern \"(.*)\" of the picking list panel next level$/, () => { });
When(/^the user checks the selected picking list panel item$/, () => { });
When(/^the user unchecks the selected picking list panel item$/, () => { });
When(/^the user expands the selected picking list panel item$/, () => { });
When(/^the user clicks the \"(.*)\" button in the left top bar of the selected picking list$/, () => { });
When(/^the output file \"(.*)\" is created$/, () => { });
When(/^the user has already selected non responsive design layout$/, () => { });
When(/^the user clicks the \"(.*)\" button in the header$/, () => { });
When(/^the user clicks the \"(.*)\" main action button on the right panel$/, () => { });
When(/^the user clicks the Close page action icon on the header panel$/, () => { });
When(/^the user clicks the \"(.*)\" action button on the header drop down$/, () => { });
When(/^the user clicks the \"(.*)\" button in the popup header$/, () => { });
When(/^the user clicks the \"(.*)\" secondary action button on the right panel$/, () => { });
When(/^the user clicks the \"(.*)\" section on the right panel$/, () => { });
When(/^the user opens the \"(.*)\" section on the right panel$/, () => { });
When(/^the user closes the \"(.*)\" section on the right panel$/, () => { });
When(/^the user clicks the \"(.*)\" link on the left panel$/, () => { });
When(/^the user clicks the \"(.*)\" tool on the right panel and selects the \"(.*)\" option$/, () => { });
When(/^the user opens the header drop down$/, () => { });
When(/^the \"(.*)\" main action button on the right panel is disabled$/, () => { });
When(/^the \"(.*)\" main action button on the right panel is enabled$/, () => { });
When(/^the \"(.*)\" action button on the header drop down is disabled$/, () => { });
When(/^the \"(.*)\" action button on the header drop down is enabled$/, () => { });
When(/^the user clicks the \"(.*)\" tab selected by title$/, () => { });
When(/^the \"(.*)\" secondary action button is disabled$/, () => { });
When(/^the \"(.*)\" secondary action button is enabled$/, () => { });
When(/^the user clicks the \"(.*)\" icon in the header$/, () => { });
When(/^the user writes to the selected \"(.*)\" text field on the sage exchange site$/, () => { });
When(/^the user selects the text field with id \"(.*)\" on the sage exchange site$/, () => { });
When(/^the user selects the drop-down list field with id \"(.*)\" on the sage exchange site$/, () => { });
When(/^the user selects the \"(.*)\" option of the selected drop-down list field on the sage exchange site$/, () => { });
When(/^the user selects the \"(.*)\" button on the sage exchange site$/, () => { });
When(/the user takes a screenshot$/, () => { });
When(/debug$/, () => { });
Then('a tracker appears signalling completion', () => { });
When(/^the tracker has the job title \"(.*)\"$/, () => { });
When(/^the tracker has the phase \"(.*)\"$/, () => { });
When(/^the tracker has the message \"(.*)\"$/, () => { });
When(/^the user clicks to dismiss the tracker$/, () => { });
When(/^the user clicks \"(.*)\" button on the tracker$/, () => { });